// app/context/IpContext.tsx
// import { createContext, useContext } from 'react';

// type IpContextType = {
//   ip: string;
// };

// // Create context with a default value
// const IpContext = createContext<IpContextType>({ ip: '' });

// // Export provider and custom hook for easy access
// export const useIp = () => useContext(IpContext);
// export default IpContext;
