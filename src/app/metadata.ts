const baseUrl = "https://cyverse.org";

const metadata = {
  title: "AWS Manager",
  description: "Start/Stop Create/Destroy AMI service",
  image: "opengraph-image.png",
  url: baseUrl,
};

export default metadata;
